# Pinned dependencies for the Feishu/Lark <-> Claude Code bridge.
# Install into the project venv with:
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# Direct dependencies the code imports:
#   lark-oapi      — Feishu/Lark official SDK (REST client + WebSocket long-connection)
#   requests       — HTTP transport used by the SDK / helpers
#   websockets     — WebSocket client used by lark-oapi's long-connection mode
# Everything else below is a transitive dependency, pinned for reproducibility.
lark-oapi==1.7.1
requests==2.34.2
websockets==15.0.1

# --- transitive (pinned; re-resolve with `pip install <direct> && pip freeze` if you bump versions) ---
anyio==4.14.1
certifi==2026.6.17
charset-normalizer==3.4.9
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
numpy==2.5.1
pycryptodome==3.23.0
requests-toolbelt==1.0.0
urllib3==2.7.0

# --- mcp-bridge (Feishu MCP channel) ---
# Direct: the MCP Python SDK used by mcp_channel/.
mcp==1.28.1
# Transitive deps of mcp==1.28.1 (pinned for reproducibility):
annotated-types==0.7.0
attrs==26.1.0
cffi==2.1.0
click==8.4.2
cryptography==49.0.0
httpx-sse==0.4.3
jsonschema==4.26.0
jsonschema-specifications==2025.9.0
pycparser==3.0
pydantic==2.13.4
pydantic-core==2.46.4
pydantic-settings==2.14.2
pyjwt==2.13.0
python-dotenv==1.2.2
python-multipart==0.0.32
referencing==0.37.0
rpds-py==2026.6.3
sse-starlette==3.4.5
starlette==1.3.1
typing-extensions==4.16.0
typing-inspection==0.4.2
uvicorn==0.51.0
