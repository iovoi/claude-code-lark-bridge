# Dependencies for the Feishu/Lark <-> Claude Code bridge.
# Canonical source is pyproject.toml (direct dep: lark-oapi). This file is kept for
# `pip install -r requirements.txt` convenience — it just installs the project editable,
# which pulls lark-oapi and its transitive deps (requests, websockets, anyio, …).
-e .
